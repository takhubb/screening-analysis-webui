services:
  web:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      PYTHONPATH: /app/src
    volumes:
      - ./.cache:/app/.cache
    restart: unless-stopped
